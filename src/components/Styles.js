import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  todasTelas: {
    backgroundColor: '#fff',
    height: '100%'
  },
  fonteMaior: {
    color: "#fff",
    fontSize: 15
  }
});

export default styles;
